# --- YouTube Collector Environment Variables ---
# Copy this file to .env and fill in your values.
# .env is ignored by git.

# --- Core ---
# DOWNLOAD_DIR=path/to/your/downloads # Optional: Override default 'downloads' directory
# LOG_LEVEL=DEBUG # Optional: Override default 'INFO' log level (DEBUG, INFO, WARNING, ERROR)

# --- YouTube Settings ---
# MAX_SEARCH_RESULTS=50 # Optional: Override default 100

# --- LLM Provider ---
# Required for filtering/enhancement. E.g., 'openai', 'google', 'anthropic'
# LLM_PROVIDER=openai
LLM_API_KEY=

# Optional: Override default models
# LLM_MODEL_FILTERING=gpt-4
# LLM_MODEL_ENHANCEMENT=gemini-1.5-pro

# --- Downloader ---
# Optional: Rate limit in KiB/s (e.g., 5120 = 5 MiB/s)
# DOWNLOAD_RATE_LIMIT_KIBPS=5120

# Optional: Comma-separated list of subtitle languages (yt-dlp format)
# Pydantic BaseSettings will handle parsing this into a list
# DOWNLOAD_SUB_LANGS=en.*,en,es,fr

# --- Persistence ---
# Optional: Override default persistence file path
# PERSISTENCE_FILE=data/video_log.json
